name: tool_clind_network
description: tool clind network package.
publish_to: none

environment:
  sdk: ^3.5.3
  flutter: '>=1.17.0'

dependencies:
  flutter:
    sdk: flutter

  core_network:
    path: ../../../core/network
