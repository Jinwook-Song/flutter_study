name: notification_data
description: feature notification data package
publish_to: none

environment:
  sdk: ^3.5.3
  flutter: '>=1.17.0'

dependencies:
  notification_domain:
    path: ../domain

  core_util:
    path: ../../../core/util

  tool_clind_network:
    path: ../../../tool/clind/network
