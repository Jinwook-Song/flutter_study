name: monorepo_starter
description: 'A new Flutter project.'
publish_to: none

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  add:
    path: ../../packages/add
  setting:
    path: ../../packages/setting

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
